# Memory Game

## Why this exists

A console memory game where the player has to remember positions or match pairs.

## What this project shows

It shows my ability to manage dynamic game state, work with arrays, and design simple but engaging game logic.

## How to run (local)

Open this folder in your terminal and compile with GCC or your C compiler.

Single program example:
  gcc main.c -o app -mconsole
  ./app   (or .\app.exe on Windows)

For folders with multiple small programs:
  gcc file_name.c -o file_name.exe -mconsole
  .\file_name.exe
